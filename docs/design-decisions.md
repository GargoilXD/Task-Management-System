# Day 1
- Defined the fields for the `Project` Class
- Used `ToString()` to display project details
- Initialize the `Task` Array in the Class definition instead of the constructor
- added `OperationResult` enum
- added `MAX_TASK_SIZE` constant variable
- modified the `OperationResult` enum to a class